import styled from "styled-components";

export const WeekRow = styled.div`
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-around;
    background: inherit;
    width: 100%;
`;
