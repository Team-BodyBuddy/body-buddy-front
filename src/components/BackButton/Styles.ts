import styled from "styled-components";

export const BackButton = styled.button`
  background: none;
  border: none;
  cursor: pointer;
  display: flex;
  margin-left: 1.25rem;
  margin-top: 1.25rem;
  align-items: center;
  justify-content: center;

  svg {
    width: 1.5rem;
    height: 1.5rem;
    color: #333;
  }
`;
