import { UpdatePortfolioResponse } from "./types";

export const mockUpdatePortfolioResponse: UpdatePortfolioResponse = {
    isSuccess: false,
    code: "ERROR",
    message: "Failed to update portfolio",
    result: {},
  };